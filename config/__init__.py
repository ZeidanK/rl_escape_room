"""Expose configuration dataclasses and room metadata for app and scripts."""

# Configuration objects and room metadata used by scripts/UI.
from config.settings import RLConfig, TrainingResult, EpisodeRecord
from config.rooms import ROOM_SPECS
